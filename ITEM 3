#include <stdio.h>
#include <stdlib.h>
int main() {
    int celsius;
    int fahrenheit;

    printf("Quantos graus Celsius: ");
    scanf("%d", &celsius);

    fahrenheit = (celsius * 9) / 5 +32.;

    printf("Temperatura em Fahrenheit e: %d\n", fahrenheit);

    return 0;
}
