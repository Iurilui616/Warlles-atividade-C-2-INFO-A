#include <stdio.h>
#include <stdlib.h>
int main() {
    int fahrenheit;
    int celsius;

    printf("Temperatura em Fahrenheit: ");
    scanf("%d", &fahrenheit);

    celsius = ((fahrenheit - 32) * 5) / 9;

    printf("Temperatura em Celsius e: %d\n", celsius);

    return 0;
}
